# rpg-standx-game

StandX RPG migrated to Next.js.

## Commands

- `npm run dev` starts the Next.js dev server.
- `npm run build` creates a production build.
- `npm run lint` runs ESLint over the app sources.
